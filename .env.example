# Copy to .env.local and fill in — `npx convex dev` sets these for you.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Absolute origin for metadata, sitemap and robots.txt (defaults to the Vercel URL, then localhost)
NEXT_PUBLIC_SITE_URL=
